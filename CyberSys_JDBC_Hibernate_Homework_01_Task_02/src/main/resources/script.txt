CREATE DATABASE IF NOT EXISTS companydb;

USE companydb;

CREATE TABLE IF NOT EXISTS Employees (Id INT AUTO_INCREMENT PRIMARY KEY, FullName VARCHAR(100) NOT NULL, Phone VARCHAR(20));

CREATE TABLE IF NOT EXISTS EmployeeDetails (PersonalId INT  AUTO_INCREMENT PRIMARY KEY,
                                            EmployeeId INT,
                                            MaritalStatus VARCHAR(50),
                                            BirthDate DATE,
                                            Address VARCHAR(300),
                                            FOREIGN KEY (EmployeeID) REFERENCES Employees(Id));
CREATE TABLE IF NOT EXISTS Salaries (Id INT AUTO_INCREMENT PRIMARY KEY,
    EmployeeId INT,
    Position VARCHAR(100),
    Salary DECIMAL(10,2),
    FOREIGN KEY (EmployeeID) REFERENCES Employees(Id)
);

INSERT INTO Employees (FullName, Phone)
VALUES ('Олег Суходрев', '+380671112233');

INSERT INTO Employees (FullName, Phone) VALUES ('Іван Шевкуненко', '+380512345676');

INSERT INTO Salaries(EmployeeId, Position, Salary) VALUES (1, 'Product owner', 15000);

INSERT INTO Salaries(EmployeeId, Position, Salary) VALUES (2, 'QA Ingeneer', 9000);

INSERT INTO EmployeeDetails(EmployeeId, MaritalStatus, BirthDate, Address) VALUES (1, 'Неодружений', '1990-05-10', 'Київ');

INSERT INTO EmployeeDetails(EmployeeId, MaritalStatus, BirthDate, Address) VALUES (2, 'Одружений', '1985-03-15', 'Львів');